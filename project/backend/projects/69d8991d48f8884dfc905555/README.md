# ecommerce webste
## Auto-Generated by Universal AI Build Engine
- Category: GRADUATION | Complexity: INTERMEDIATE
- Backend: Core PHP | Frontend: HTML, CSS & JS | DB: MySQL / MariaDB

## Quick Start
# Backend (Port 5000)
cd backend && npm install && npm run dev
# Admin Panel (Port 3001)
cd admin && npm install && npm run dev
# Frontend (Port 3000)
cd frontend && npm install && npm run dev

## Architecture
erDiagram
    USER ||--o{ ECOMMERCE : manages
    ECOMMERCE ||--|{ RECORD : contains
    USER {
      string id
      string name
      string email
    }
    ECOMMERCE {
      string id
      string title
      date created_at
    }
