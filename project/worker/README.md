# Future BRTS Python Worker

This is the dedicated Python worker for Future BRTS, responsible for executing heavy verification tasks:
- Code Execution
- MCQ Validation
- Viva Scoring
- Logic Evaluation

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the worker: `python main.py`
