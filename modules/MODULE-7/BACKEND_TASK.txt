TASK: Cloud Orchestrator Engine Backend

Goal:
Generate backend to build & deploy all microservices locally via docker-compose.

Includes:
- Dockerfile generator (Next.js / Express TS / Python Worker)
- docker-compose.yml generator
- nginx.conf generator
- Local deploy scripts: build-images.sh, up.sh, down.sh, logs.sh
- Orchestrator APIs:
    GET /infra/services
    POST /infra/service/{name}/action
    POST /infra/deploy
    GET /infra/deploy/{id}/status
    GET /infra/logs

Output: output_v7_BACKEND.md
