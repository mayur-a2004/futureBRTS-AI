TASK: Generate Express + TypeScript API Gateway + MongoDB + Redis Queue

Goal:
Create runnable backend skeleton:
- Express + TS + MongoDB + Redis integration
- Routes:
  POST /project/build
  GET  /project/build/:id/status
  GET  /project/download/:id
  PATCH /blueprint/:id/features
  POST /blueprint/:id/table
  PATCH /blueprint/:id/column
  POST /blueprint/:id/relation
  PATCH /blueprint/:id/theme

- Services:
  blueprintService
  buildService (enqueue job to Redis)

- utils:
  mongoClient.ts
  redisClient.ts
  logger.ts
  errorHandler.ts

Output: output_v6_BACKEND.md
